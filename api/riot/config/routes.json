{
  "routes": [
    {
      "method": "GET",
      "path": "/summoner/:summoner",
      "handler": "summoner.findSummoner",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/games/:puuid",
      "handler": "summoner.findGamesBySummoner",
      "config": {
        "policies": []
      }
    }
  ]
}
